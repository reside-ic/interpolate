declare module "solve-tridiagonal" {
    export default function solveTridiagonal(n: number, a: number[],
                                             b: number[], c: number[],
                                             x: number[]): boolean;
}
